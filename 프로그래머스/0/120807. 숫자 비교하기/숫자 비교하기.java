class Solution {
    public int solution(int num1, int num2) {
        // 두 수가 같으면 1, 다르면 -1을 반환합니다.
        if (num1 == num2) {
            return 1;
        } else {
            return -1;
        }
    }
}
